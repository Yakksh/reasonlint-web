# ReasonLint Web Configuration
# Copy this file to .env or .env.local and update the values

# Base URL for the deployed site
# This is used for install script URLs and other full URLs
# Examples:
#   - https://reasonlint.com
# If not set, the app will auto-detect from the current URL at runtime
VITE_BASE_URL=
